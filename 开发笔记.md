# 开发笔记

这份文档给**想改这个工具的人**看（包括几个月后的我自己）。用户怎么用请看 [README](README.md)。

当前版本：**Windows v1.7 / 安卓 v1.7**

---

## 1. 源码结构

```
SICAU-AutoLogin/
├── SICAU-win-<版本>.exe        Windows 程序本体（构建脚本会更新这一份）
├── 校园网自动认证.lnk           「启动」文件夹里的开机自启快捷方式（不在仓库里，是程序建的）
├── Windows使用手册.docx / 安卓使用手册.docx      给用户看的图文手册
├── README.md / 开发笔记.md / LICENSE / .gitignore
├── android/
│   └── SICAU-android-<版本>.apk
└── build/                     所有源码和构建工具
    ├── App.cs                  Windows 界面 + 主逻辑（托盘、向导、热点、关于）
    ├── Core.cs                 Windows 认证 / 配置 / 自启 / 日志 / 门户探测开关
    ├── Ui.cs                   自绘控件（Win11 风格按钮、卡片、字体）
    ├── HotspotScript.ps1       热点操作脚本（编译时嵌进 EXE）
    ├── app.ico / app.manifest  图标 / DPI 感知清单
    ├── avatar.png              作者头像（编进 EXE，「关于」对话框用）
    ├── avatar-source.jpg       头像源图（方形，用来重新生成上面那个）
    ├── build-win.ps1           ★ Windows 构建
    ├── build-apk.ps1           ★ 安卓构建
    ├── 生成头像资源.ps1        从头像源图生成两端的资源
    ├── mkdocx.ps1              把 manual-*.txt 生成 .docx（纯 OOXML，不依赖 Office）
    ├── manual-windows.txt / manual-android.txt    手册的文本源
    ├── apk-src/                安卓源码
    │   ├── SicauAuth.java      认证协议（不依赖 android.*）
    │   ├── Http.java           自己实现的 HTTP（强制 IPv4 + 自解析证书）
    │   ├── AppLog.java         日志（写文件 + 回调界面）
    │   ├── WifiPlan.java       WiFi 名字解析/匹配/列表排序（纯 Java，有单元测试）
    │   ├── WifiSwitch.java     连接 WiFi / 权限 / 读 SSID / 诊断
    │   ├── WifiScan.java       扫周围的 WiFi
    │   ├── AppCtx.java         存一份 ApplicationContext
    │   ├── MainActivity.java   界面
    │   └── Prefs/Scheduler/AuthJobService/BootReceiver/KeepAliveService
    ├── jvmtest/                桌面测试台（真实认证 + 69 项单元测试，不用手机）
    └── sdk/                    Android SDK（不提交，自己放一份）
```

**用户数据**（不在仓库里）：

- 配置 `%APPDATA%\SICAU-AutoLogin\config.conf`（明文，含学号密码）
- 日志 `%APPDATA%\SICAU-AutoLogin\autologin.log`
- 自启 `%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\校园网自动认证.lnk`

---

## 2. 构建与发版

### Windows

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File build\build-win.ps1
```

流程：编译 → 复制到**项目根目录** → 删掉根目录下别的版本和旧名字 `SICAU-AutoLogin.exe` →
检查「启动」快捷方式有没有指偏（指偏了就改成当前程序）。**构建前先关掉正在运行的 EXE**，否则复制失败。

发版改**两处**：`build-win.ps1` 顶部的 `$VER`（决定文件名）和 `Core.cs` 里的 `AppVersion`
（决定「关于」对话框和卸载列表里显示的版本）。

编译命令本质是：

```
csc /nologo /target:winexe /platform:anycpu /optimize+ /codepage:65001
  /out:build\SICAU-win-<版本>.exe /win32icon:app.ico /win32manifest:app.manifest
  /resource:HotspotScript.ps1,HotspotScript /resource:avatar.png,avatar
  /r:System.dll /r:System.Drawing.dll /r:System.Windows.Forms.dll
  Ui.cs Core.cs App.cs
```

### Android

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File build\build-apk.ps1
```

- 版本号以 `apk-src\AndroidManifest.xml` 的 `versionName`/`versionCode` 为准（脚本自己读出来喂给 aapt2）。
- 产物 `android\SICAU-android-<版本>.apk`，并顺手删掉旧版本的 `.apk` / `.apk.idsig`。
- **JDK**：脚本会按 `$env:SICAU_JDK` → `build\JdkPath.txt` → `$env:JAVA_HOME` → 常见安装目录 → `PATH`
  的顺序自己找。找不到会打印三种解决办法。
- **Android SDK**：要 build-tools **35.0.0** + platforms/android-34，放在 `build\sdk\`。
  ⚠️ 34.0.0 自带的 d8 在这个工程上会内部报错（R8 的 bug），**必须用 35.0.0**。
- **签名**：`build\sicau.keystore`。仓库里**不含**这个文件，脚本发现没有会用 `keytool` 现生成一个。
  自己发布请换成自己的密钥 —— 同一个应用换了密钥，用户升级安装会签名冲突。

### 改用户手册

手册是「文本源 → docx」生成的，别直接改 docx（改了下次生成会被覆盖）：

```powershell
# 改 build\manual-windows.txt 或 manual-android.txt, 然后:
powershell -NoProfile -ExecutionPolicy Bypass -File build\mkdocx.ps1 build\manual-android.txt
```

`mkdocx.ps1` 是纯 OOXML + zip 拼出来的，**不依赖 Office**，写文件前会先校验 XML 合法性。
⚠️ 生成前把 Word 关掉，否则会报 `being used by another process`（目录里有 `~$*.docx` 就是开着）。

---

## 3. 怎么验证（别只读代码）

### 安卓认证逻辑 —— 不用手机

`SicauAuth.java` / `Http.java` / `AppLog.java` **不依赖任何 `android.*` API`**，能在桌面 JVM 上跑真实认证：

```powershell
$J = 'build\jvmtest'
$JDK = 'C:\Program Files\Java\jdk-21'      # 换成你自己的
& "$JDK\bin\javac.exe" -encoding UTF-8 -d "$J\out" "$J\Http.java" "$J\SicauAuth.java" "$J\AppLog.java" "$J\TestReal.java"
& "$JDK\bin\java.exe" "-Dfile.encoding=UTF-8" -cp "$J\out" cn.edu.sicau.autologin.TestReal "$env:APPDATA\SICAU-AutoLogin\config.conf"
```

会打印完整认证过程（认证页 HTTP、表单字段数、会话号、响应摘要、最终结果）。**改完安卓认证逻辑先在这跑一遍再打包。**

### 纯逻辑单元测试（69 项，不用手机）

```powershell
$J = 'build\jvmtest'
& "$JDK\bin\javac.exe" -encoding UTF-8 -d "$J\out" build\apk-src\WifiPlan.java "$J\WifiPlanTest.java"
& "$JDK\bin\java.exe" -Dfile.encoding=UTF-8 -cp "$J\out" WifiPlanTest
```

覆盖：多分隔符拆名字、通配符 `*` `?`、大小写、正则元字符当字面量、`toRegex`/`toGlob` 的区别、
扫描列表去重排序（同名留最强 / 同分保序）、校园网预设、选择列表顺序。

> 为什么把这些逻辑单独放到 `WifiPlan.java`：它不碰 `android.*`，所以**能在这台电脑上测**。
> 新增纯逻辑时优先往这里放。

### Windows 界面

`App.cs` 是 WinForms，可以反射造 `MainForm` 再 `Show()` + `PrintWindow` 截图，
或者直接读私有字段拿控件值。测「关于」对话框那种模态窗的做法见下面第 6 节的探针写法。

### 查 APK 内容

```powershell
Copy-Item android\SICAU-android-*.apk temp.zip      # .apk 不能直接 Expand-Archive, 要先改名
Expand-Archive temp.zip -DestinationPath out
# 读 classes.dex 的 UTF-8 字串, 确认新代码/新文案进去了
& build\sdk\build-tools\35.0.0\aapt2.exe dump badging <apk>      # 版本号、权限
& build\sdk\build-tools\35.0.0\apksigner.bat verify --verbose <apk>
```

> ⚠️ `static final String` 常量（比如 `SCAN_RESULTS_AVAILABLE_ACTION`）编译时会被内联进调用点，
> 在 dex 里**搜不到常量名**。判断"代码进没进包"要看类名 / 方法名 / 界面文案。

---

## 4. 想改行为时看这里（设计约定）

- **配置向后兼容**：老配置里没有的键按默认值算，不要因为缺键就报错。
  例如 `bootmode=confirm` 要归到「留在托盘监测」，`mode=auto` 由 `Core.ConcreteMode()` 收敛成具体网卡。
- **连接方式只有「有线 / 无线」两种**，没有「自动」。`mode=auto` 不再写进配置文件。
- **开机自启只能用「启动」文件夹快捷方式**，**绝对不要改回 `schtasks`**（原因见第 5 节）。
- **开机自启跟程序名字走**：`SaveCfg` 里调 `EnableAutostart(Core.ExePath(), ...)`，
  谁在跑就指向谁。所以程序换名/换位置后，跑一次构建脚本或点一次保存即可。
- **Windows 版保持单文件、免安装、零依赖**（只用 .NET Framework 自带的库）。
  头像这种图片资源要 `/resource` 编进 EXE，不要读外部文件。
- **安卓不要引第三方库**（没有 AndroidX/Gradle，就是 `javac` + `aapt2` + `d8` + `apksigner` 手搓的）。
  需要圆角/圆形图片就用系统自带的 `RoundedBitmapDrawable`、`BitmapShader` 这类。
- **界面文案、日志、注释全用中文。**
- **Windows 版界面尺寸**：按 96 DPI 画的固定像素，窗口会随内容自适应（见第 5 节的坑）。

---

## 5. 踩过的坑（改之前务必看）

| 坑 | 现象 | 处理 |
|---|---|---|
| **安卓取 WiFi IP** | `WifiInfo.getIpAddress()` 在 **Android 12 (API 31) 起废弃并固定返回 0** → 拿不到 IP → 请求不绑 WiFi、走移动流量 → 误判「已联网」或认证失败 | 走 `ConnectivityManager` → `NetworkCapabilities.TRANSPORT_WIFI` → `LinkProperties` 取 IPv4，再兜底扫网卡名 |
| **安卓读 WiFi 名字** | 8.1 起要位置权限、10 起要系统定位开关、12 起部分 ROM 有权限也只给 `<unknown ssid>`。**硬读是条死路** | 认网络改用 **IP**，名字只当锦上添花；`WifiSwitch.readSsid()` 三层尝试后返回空串不报错；再加一个诊断把卡点摊给用户看 |
| **安卓 10 起不能偷偷改 WiFi** | `addNetwork`/`enableNetwork` 直接返回 -1 | Android 10+ 用 `WifiNetworkSpecifier` + `requestNetwork()`（系统弹窗）；9 及以下保留老 API |
| **`WifiNetworkSpecifier.Builder` 没有 `addSsid`** | 按记忆写 `addSsid(String,String)` / `addSsid(MacAddress)`，按 android-34 编译**两个都找不到** | 实际是 `setSsid(String)` / `setSsidPattern(PatternMatcher)`。**安卓 API 别凭记忆写**：`jar xf android.jar '...$Builder.class'` 再 `javap` 一眼就清楚 |
| **`setSsidPattern` 吃的不是正则** | 传 Java 正则通配符不生效 | 系统用的是 glob（`PATTERN_ADVANCED_GLOB`：`*` 任意多个、`.` 任意一个、`\` 转义）。自己比对用 `WifiPlan.toRegex()`，交给系统用 `toGlob()`，第二参数不传会按字面量处理 |
| **清单里 `maxSdkVersion` 卡死权限** | `ACCESS_FINE_LOCATION` 写了 `maxSdkVersion="32"`，安卓 13+ 永远申请不到，代码却还 `checkSelfPermission` → 永远「没权限」 | 声明和运行时检查要一致；权限拆成**必需**（扫描/连接）和**可选**（只为读名字） |
| **IPv6 不通** | `portal.sicau.edu.cn` 的 DNS 先回 IPv6 但那条路是死的（curl -6 超时、-4 只要 49ms），.NET 的 HttpWebRequest 不会回退 | 自己解析 IPv4、请求发到 IP 上、用 Host 头带真实域名（TLS SNI 和证书校验仍然正确） |
| **通配符证书** | 服务器是 `*.sicau.edu.cn`，JDK 默认校验器误判不匹配 | 两端都改成自己解析 SAN 列表做校验 |
| **Windows 自启不能用 `schtasks`** | Defender 把「未签名程序 + 建登录任务 + 写 TaskCache」判成 `Behavior:Win32/Persistence.A!ml`（严重性 5），**把 EXE 和计划任务一起删掉** | 改用「启动」文件夹快捷方式（`Core.EnableAutostart`），不需要管理员、不碰注册表。程序每次启动还会顺手清一次老任务 |
| **热点频段每次都重设** | `HotspotBand != "auto"` 不是「变了」，是「只要不是 auto 就每次重设」→ 每次保存都重启热点、踢设备 | 保存时现读系统值逐项比对（`BandDiffers` / `BandIndexOfSystem`），只有真的不同才写 |
| **WinForms `header.PreferredSize` 不能用来算窗口宽** | 贴右边缘的控件会让它恒等于当前宽度 → 「需要多宽」永远等于「现在多宽」，窗口再也瘦不回去 | 按内容算：左边标题的 `PreferredSize + Margin +` 右边那一组的 `PreferredSize`。靠右对齐挂在 `SizeChanged` 上，**不能挂 `Layout`**（改子控件位置会再触发 Layout → 递归栈溢出） |
| **三个页签对齐** | 每页各用一张 AutoSize 表格时，标签文字宽一点整排就左右跳（差 4px、行高差 10px） | 三个页签**合并成同一张表**，靠把行高设成 0 来切换；两个标签列固定宽度 |
| **量 144 DPI 的界面尺寸** | 测试台不带 `/win32manifest:app.manifest` 时进程是 DPI 不感知的，只能量到 96 DPI 的虚拟尺寸（两个数差 1.35 倍） | 量尺寸的测试台也要带 app.manifest |
| **固定高度的日志框看着像「不刷新」** | 日志其实写进去了，但 180dp 的 ScrollView 停在最上面几行，新行追加在下面看不见 | 每写一行就 `logScroll.fullScroll(View.FOCUS_DOWN)`（要 `post`，等布局完成再滚） |
| **`LinkProperties.getDomains()` 返回的是 String** | 按 `for (String d : lp.getDomains())` 写会报「for-each 不适用于表达式类型」 | 它返回一个字符串（可能逗号分隔），先 `split(",")` 再遍历 |
| **Word 开着的时候改不了 docx** | `mkdocx.ps1` 报 `being used by another process` + `Cannot create a file when that file already exists`，还会留下 `.docx.tmp` | 看目录有没有 `~$<名字>.docx` 锁文件；让用户关掉 Word 再生成，并清掉 `.tmp` |
| **PowerShell 里的反引号和中文引号** | 用代码模式写 PS 脚本时，PS 的续行符反引号会和 JS 模板字符串定界符冲突；中文引号「"」会破坏 PS 字符串 | 命令写成单行不用续行符；PS 字符串里别用中文引号；必要时用占位符替换 |
| **`csc` 忘了定义变量** | `/out:` 变成相对路径，报「找不到源文件 \Ui.cs」 | 编译命令里先 `$D = '<绝对路径>'`，或者用 `$PSScriptRoot` |
| **构建脚本写死路径** | `$D` / `$DESK` 写死某台机器的绝对路径，项目一搬地方脚本就废 | 统一 `$D = $PSScriptRoot`；JDK 按多个来源自动找（见第 2 节） |
| **`.apk` 解压** | `Expand-Archive` 不认 `.apk` 扩展名 | 先复制成 `.zip` |
| **`--auto` 不一定真的提交门户** | 网络本来就通时 `--auto` 会短路成「当前已联网, 无需认证」，日志里看不到提交过程 | 想验证认证链路，用探针调 `Core.DoAuth(cfg, force:true)`。注意它是**真提交**，跑之前先备份 config |

### 几个测试台写法上的坑

- **模态对话框的截图**：`ShowDialog(owner)` 创建的是**有 owner 的窗口**，
  `EnumWindows`（只枚举无主顶层窗口）**找不到它**，用 `EnumThreadWindows(GetCurrentThreadId(), ...)`。
- **截图时机**：`ShowDialog` 是阻塞的，所以要先用 `Timer` 把「调用 ShowAbout」排进消息循环，
  再用另一个 `Timer` 去截。截完直接 `Environment.Exit(0)` —— `Application.Exit()` 关不掉模态框。
- **测试台的 Main 会跟 App.cs 打架**：编译时加 `/main:Sicau.XxxProbe.Program`。
- **只测 `Core.cs`** 不用带 `App.cs`/`Ui.cs`；要测界面才带上，并且带上 `app.manifest`（DPI 感知）。

---

## 6. 开发史（按时间倒序）

> 早期几版是边做边试出来的，记的是**当时**的现象和结论，里面的路径/版本可能已经不是现在的样子。

### v1.7 — 开关集成进程序 + 安卓界面精简 + 作者标识

- **Windows**：`Core.ProbeEnabled()/SetProbe()/ProbeInfo()` 读写 NCSI 的 `EnableActiveProbing`；
  界面加勾选项「不自动弹校园网登录页」；没管理员权限时弹窗问要不要 `runas` 提权重开。
  写进去后会**再读一遍确认**，被组策略锁住时不会静默失败。
- **安卓**：去掉 4 个重复控件 —— 「检测状态」按钮（认证/日志本来就会说）、
  「选择」按钮（改成点输入框弹列表）、「系统设置」按钮（挪进列表里）、
  「诊断」按钮（改成点 WiFi 状态文字）；按钮行只剩「保存并应用 / 立即认证」。
  新增「减少『登录到网络』打扰」——**这个改不了系统设置**，勾上会告诉用户去哪儿关通知
  （Android 侧第三方应用没权限关强制门户检测）。
- **作者标识**：`build\avatar.png`（128px 圆形）编进 EXE，「关于」对话框左边显示头像 + 「极夜光」；
  安卓主界面标题左边是圆形头像，下面一行「极夜光 · yeguang225@outlook.com」。
  头像资源用 `生成头像资源.ps1` 生成 —— 安卓那边用**方形 JPEG + 运行时裁圆**，
  比带透明的圆形 PNG 小 6 倍（160px：47KB → 7.7KB）。

### v1.6 — 学校那两个 WiFi 名做成按钮

在用户电脑上用 `netsh wlan show profiles` / `netsh wlan show networks mode=bssid` 查到就两个：
`i_sicau_wifi6`、`i_sicau`（这个只在本项目适用，别人 fork 要改成自己学校的）。
`WifiPlan.CAMPUS_PRESETS` 是唯一写死名字的地方；界面上做成两个按钮，点一下直接连。

### v1.5 — 「读不到 WiFi 名字」也能用

真机反馈读不到 SSID。改成**认网络看 IP**，名字读不到就如实显示并给诊断；
顺手修了个真 bug：清单里 `ACCESS_FINE_LOCATION` 带 `maxSdkVersion="32"` 导致安卓 13+ 上永远「没权限」。

### v1.4 — 产物改名 + WiFi 名字改「选择」模式

产物统一成 `SICAU-win-<版本>.exe` / `SICAU-android-<版本>.apk`；
`build-apk.ps1` 改成从清单读版本号；自启快捷方式跟着程序名字走。
安卓加了扫描列表（`WifiScan.java`），去重排序逻辑挪进 `WifiPlan` 好上单元测试。

### v1.3 — 连接 WiFi + 日志实时刷新

「切换 WiFi」原来是个**假按钮**（只跳系统设置）；日志问题其实是**没自动滚到底**。
加了 `WifiSwitch.java`（连接/权限/读状态）和 `WifiPlan.java`。

### v1.3（Windows）— 连接方式取消「自动」

原来 `mode` 有 `auto`/`wired`/`wifi`。`auto` 不绑网卡、只看系统路由通不通，
于是「宿舍有线在线」时直接判定「已联网，无需认证」，无线那张卡永远不被认证。
**改成页签二选一**，老配置的 `auto` 由 `Core.ConcreteMode()` 收敛成具体值。

### v1.3（Windows）— 界面重排 + Win11 配色 + 窗口缩小

三个页签合并成一张表解决对齐；标题栏右上角放「界面主题」「认证成功后」；
改仿 Win11 的浅色/深色配色（深色的输入框填充色要比卡片**亮**）；
顺手修了首次向导「环境检测」因 `InterfaceIPv4()` 返回 null 而显示「检测失败」的 bug。

### v1.3（Windows）— 「认证成功后」现在管得住手动认证和点 X

这个设置原来只管开机那条路：`DoAuthAsync()`（窗口/托盘点立即认证）和
`OnFormClosing()`（点 X）都没读它。现在两条路都跟着 `SelectedBootMode()` 走。

### v1.3（Windows）— 日志自动清理 + 开机自启提速 + 改用「启动」文件夹

- 配置项 `logkeephours`（默认 24，0 = 不删），`Core.Log()` 写完一行后**每小时最多扫一次**文件。
- 开机自启先去掉计划任务的 20 秒延迟、加 `Core.WaitForNetwork()`、进程优先级提到 High；
  **紧接着程序整个被杀软删了** —— 于是彻底改用「启动」文件夹快捷方式（见第 5 节）。

### 更早

- **v1.2**：修安卓 WiFi IP 获取；热点频段不再每次重设。
- **v1.1 / 初版**：把认证协议跑通（两平台）、Windows 单文件 EXE + Win11 风格界面、托盘、首次向导、
  热点分享、安装/卸载。

---

## 7. 还没解决 / 待确认

1. **安卓 v1.3 之后的改动都只验到「编译 + 打包 + 签名 + 桌面单元测试」**。
   系统弹窗长什么样、扫描列表能列出几条、日志滚不滚、开机广播收不收得到 —— 只有真机能验。
2. **「读不到 WiFi 名字」的真实原因没定论**（是定位开关没开、权限没给、还是 ROM 不给名字）。
   程序已经改成不依赖名字，但想知道原因得看用户点状态文字后的诊断截图。
3. **安卓后台服务没做「自动连 WiFi」**（`KeepAliveService` / `JobScheduler` 用的是当时连着的网）。
   要不要加，看实际需求。
4. **Windows 的「不自动弹登录页」没实测过真正写入**（开发时拿不到管理员权限，
   只验到「没权限时体面失败、不误改」）。用户跑过一次才知道效果。
5. 界面上的「清空日志」按钮**只清界面显示，不动文件**；清文件靠「日志保留」。

---

## 8. 已知的平台限制（用户问到时别答错）

| 问题 | 答案 |
|---|---|
| 能不能让安卓不弹「登录到网络」 | **第三方应用做不到**。那是系统的强制门户检测（`com.android.captiveportallogin`）。只能引导用户关通知，或用 adb 关掉整个门户检测 |
| 能不能让安卓静默切 WiFi | **安卓 10 起不可能**。必须系统弹窗用户点一下 |
| 能不能读 WiFi 名字 | 要位置权限 + 系统定位开关（10 起）；12 起部分 ROM 有权限也不给。**程序不依赖它** |
| 能不能不往桌面放快捷方式 | 本来就不放。程序在哪跑就在哪，构建脚本只更新项目根目录那份 |
| 卸载会不会删配置 | 会单独问一次；默认保留配置和日志 |
