# 把项目发布到 GitHub —— 请你做两件事

本地仓库已经准备好了（56 个文件，SDK 和编译产物都排除在外）。要推上 GitHub，需要你做两件事。

---

## 第一件：给我一个 GitHub 的「钥匙」（访问令牌）

GitHub 不允许用账号密码推送，要用一个**访问令牌（token）**代替密码。生成步骤：

1. 打开这个页面（你浏览器已登录 GitHub）：

   **https://github.com/settings/personal-access-tokens/new**

2. 按下面填：

   | 栏目 | 填什么 |
   |---|---|
   | **Token name**（名字） | 随便，例如 `校园网工具上传` |
   | **Expiration**（有效期） | 选 **90 days** 就行（到期了再生成一个） |
   | **Repository access** | 选 **All repositories** |
   | **Permissions** → 展开 **Repository permissions** | 找到 **Administration** → 选 **Read and write**（这一项是为了让它能帮你**建仓库**）<br>再找到 **Contents** → 选 **Read and write**（这一项是上传代码） |

   > 只改这两项，其他保持 No access 就行。

3. 拉到底点 **Generate token**
4. 页面上会出现一串 `github_pat_xxxxxxxx...`，**这串只显示一次**，复制它

5. **把它存成一个文件**（别贴在聊天里）：

   在文件管理器地址栏输入 `%TEMP%` 回车，进去新建一个文本文件，
   命名成 **`gh-token.txt`**，把令牌粘进去保存（一行就行，别有多余空格）。

   > 为什么让你存文件：贴聊天里它会留在对话记录里；存文件我读完就能删掉。
   > 这个令牌等于你仓库的钥匙，用完我提醒你作废。

---

## 第二件：告诉我几件事

1. **仓库名字**想叫什么？我建议 `SICAU-AutoLogin`（也可以叫别的）
2. **公开还是私有**？分享给别人用的话选**公开（Public）**
3. **要不要把你的程序也传上去**？
   GitHub 有个「Releases」功能专门放编译好的成品，别人直接下载就能用。
   要的话我把 `SICAU-win-1.7.exe` 和 `SICAU-android-1.7.apk` 一起传上去。

---

## 我接下来会做什么

1. 用你的令牌**建仓库**（就在你账号下，公开的话别人能搜到）
2. **推送代码**（56 个文件）
3. 如果你要，**建一个 v1.7 的 Release** 并附上两个成品文件
4. 把仓库地址发给你，你用浏览器打开就能看到
5. 提醒你把令牌作废（或者你直接删掉那个文件也行）

---

## 顺便说一下：`git` 我装在哪儿了

你机器上本来没装 git，我下了一个**免安装版**放在临时目录里，够我用。
但**以后你自己想提交修改**（比如改了代码想更新到 GitHub），需要有个正经的 git。
我正在用 winget 装一个正式的 Git for Windows；装好后你就能在 PowerShell 里直接敲 `git` 命令了。

如果装不上，我也可以把免安装版挪到一个固定位置给你用，到时候告诉你。
